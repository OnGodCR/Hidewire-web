{
  "name": "hidewire-web",
  "version": "1.0.0",
  "private": true,
  "description": "hidewire.org: waitlist, terms, privacy. Static, no dependencies.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "serve": "node tools/serve.mjs",
    "dev": "node build.mjs && node tools/serve.mjs",
    "check": "node build.mjs --check-only",
    "test": "node tools/waitlist.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
